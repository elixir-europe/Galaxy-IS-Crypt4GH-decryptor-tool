<tool id="crypt4ghdecryptortool" name="Crypt4GH Decryptor" version="0.1.0" python_template_version="3.5">
    <requirements>
        ## Iirc we don to need any requrement
    </requirements>
    <command detect_errors="exit_code">
        touch decryptedFile.txt
    </command>
    <inputs>
       ## <param name="input1" type="data" label="Input" help="Input for the python decrypt script" />
    </inputs>
    <outputs>
        ## <data name="outfile" label="${tool.name} on ${on_string}" format="Insert desired format">
    </outputs>
    <macros>
        <xml name="bio_tools">
            <xrefs>
                <xref type="bio.tools">Bellerophon</xref>
            </xrefs>
        </xml>
    </macros>
    <help>
        <description>Crypth4GH Decryption Tool</description>
    </help>
</tool>
